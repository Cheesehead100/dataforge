{#
  ansible/requirements.yml.j2 — Ansible Galaxy collection dependencies.
  Installs: databricks.databricks (>=1.3.0) for workspace configuration tasks and
  azure.azcollection (>=2.0.0) for Azure resource management tasks.
  Context vars: product_name.
  Env logic: none.
  Constraint: Install with ansible-galaxy collection install -r ansible/requirements.yml
  before running any DataForge Ansible playbooks.
#}
---
# DataForge Ansible Requirements — {{ product_name }}
# Generated by DataForge AnsibleGenerator (L7)
# Install with: ansible-galaxy collection install -r ansible/requirements.yml
collections:
  - name: databricks.databricks
    version: ">=1.3.0"
  - name: azure.azcollection
    version: ">=2.0.0"
