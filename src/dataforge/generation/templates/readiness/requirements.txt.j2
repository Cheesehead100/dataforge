{#
  readiness/requirements.txt.j2 — Python dependencies for the readiness test suite.
  Includes: pytest + timeout plugin (always); azure-storage-blob + azure-identity
  (when has_adls); databricks-sdk (when has_databricks); azure-keyvault-secrets
  (when has_key_vault).
  Context vars: product_name, has_adls, has_databricks, has_key_vault.
  Env logic: none.
  Constraint: Install with pip install -r tests/readiness/requirements.txt before
  running pytest tests/readiness/.
#}
# DataForge Readiness Test Dependencies — {{ product_name }}
# Generated by DataForge ReadinessGenerator (L8)
# Install: pip install -r tests/readiness/requirements.txt
pytest>=8.0
pytest-timeout>=2.3
{% if has_adls %}
azure-storage-blob>=12.19
azure-identity>=1.15
{% endif %}
{% if has_databricks %}
databricks-sdk>=0.20
{% endif %}
{% if has_key_vault %}
azure-keyvault-secrets>=4.7
{% endif %}
