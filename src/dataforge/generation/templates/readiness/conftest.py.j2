{#
  readiness/conftest.py.j2 — pytest fixtures for platform readiness tests.
  Provides: session-scoped fixtures for BlobServiceClient, Databricks WorkspaceClient,
  and Key Vault SecretClient; product_name and environment string fixtures.
  Context vars: product_name, env, has_adls, has_databricks, has_key_vault.
  Env logic: none — fixture availability is controlled by has_* flags.
  Constraint: All connection details are read from environment variables exported
  from terraform output; tests skip gracefully when required vars are missing.
#}
"""
Readiness test fixtures — {{ product_name }} ({{ env }})
Generated by DataForge ReadinessGenerator (L8)

Run after Ansible configuration completes:
  pip install -r tests/readiness/requirements.txt
  pytest tests/readiness/ -v --tb=short

All connection details are read from environment variables produced by:
  terraform output -json > /tmp/tf_outputs.json
  source tests/readiness/export_tf_outputs.sh
"""

from __future__ import annotations

import os
import pytest

{% if has_adls %}
from azure.storage.blob import BlobServiceClient
from azure.identity import DefaultAzureCredential
{% endif %}
{% if has_databricks %}
from databricks.sdk import WorkspaceClient
{% endif %}
{% if has_key_vault %}
from azure.keyvault.secrets import SecretClient
{% endif %}


def _require(var: str) -> str:
    val = os.environ.get(var, "")
    if not val:
        pytest.skip(f"Environment variable {var!r} not set — skipping")
    return val


{% if has_adls %}
@pytest.fixture(scope="session")
def storage_account_name() -> str:
    return _require("STORAGE_ACCOUNT_NAME")

@pytest.fixture(scope="session")
def blob_client(storage_account_name: str) -> BlobServiceClient:
    credential = DefaultAzureCredential()
    return BlobServiceClient(
        account_url=f"https://{storage_account_name}.dfs.core.windows.net",
        credential=credential,
    )
{% endif %}

{% if has_databricks %}
@pytest.fixture(scope="session")
def dbx() -> WorkspaceClient:
    host = _require("DATABRICKS_HOST")
    token = _require("DATABRICKS_TOKEN")
    return WorkspaceClient(host=host, token=token)
{% endif %}

{% if has_key_vault %}
@pytest.fixture(scope="session")
def kv_client() -> SecretClient:
    vault_uri = _require("KEY_VAULT_URI")
    return SecretClient(vault_url=vault_uri, credential=DefaultAzureCredential())
{% endif %}

@pytest.fixture(scope="session")
def product_name() -> str:
    return "{{ product_name }}"

@pytest.fixture(scope="session")
def environment() -> str:
    return "{{ env }}"
